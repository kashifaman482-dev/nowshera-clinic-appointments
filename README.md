  # Nowshera Family Clinic
  Run locally: npm install, copy .env.example to .env and fill in the values, then npm run dev (localhost:3000).
  Stack: React + Vite, Supabase (auth, Postgres, RLS), n8n (emails and automation).
